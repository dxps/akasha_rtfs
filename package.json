{
  "name": "akasha",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "backend",
    "frontend/web",
    "frontend/mobile",
    "shared"
  ],
  "scripts": {
    "dev:backend": "bun --filter @akasha/backend dev",
    "dev:web": "bun --filter @akasha/web dev",
    "dev:mobile": "bun --cwd frontend/mobile expo start --android",
    "dev:mobile:metro": "bun --cwd frontend/mobile expo start",
    "dev:mobile:ios": "bun --cwd frontend/mobile expo start --ios",
    "build": "bun --filter '*' build",
    "typecheck": "bun --filter '*' typecheck"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  },
  "engines": {
    "bun": ">=1.2.0"
  }
}
